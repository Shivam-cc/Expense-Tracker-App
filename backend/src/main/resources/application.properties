# Application
spring.application.name=expense-tracker-api
server.port=${PORT:8080}

# Active profile: 'local' for dev (H2), 'prod' for deployment (PostgreSQL)
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}

# JWT
app.jwt.secret=${JWT_SECRET:dGhpc2lzYXZlcnl2YWxpZGI2NHNlY3JldGtleWZvcmp3dGhzbWFydGV4cGVuc2V0cmFja2VyYXBwbGljYXRpb24=}
app.jwt.expiration-ms=86400000

# CORS — comma-separated list of allowed origins (supports * wildcards via setAllowedOriginPatterns)
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,https://*.vercel.app}

# OTP verification — set OTP_DEV_MODE=true in Railway to skip SMTP for debugging
app.otp.dev-mode=${OTP_DEV_MODE:false}

# Email (Resend API — https://resend.com)
# Set RESEND_API_KEY in Railway env vars. When absent, OTP is only logged (dev mode).
app.resend.api-key=${RESEND_API_KEY:}
app.resend.from=${RESEND_FROM:onboarding@resend.dev}

# JPA common
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
